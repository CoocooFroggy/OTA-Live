apiVersion: v1
kind: Service
metadata:
  name: {{ include "ota-live.fullname" . }}
  labels:
    {{- include "ota-live.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ota-live.selectorLabels" . | nindent 4 }}
