apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ota-live.fullname" . }}-test-connection"
  labels:
    {{- include "ota-live.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ota-live.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
