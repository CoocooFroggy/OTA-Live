trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: Docker@2
      displayName: Build an image
      inputs:
        command: build
        dockerfile: '$(Build.SourcesDirectory)/Dockerfile'
        tags: |
          $(tag)
    - task: CmdLine@2
      inputs:
        script: 'docker save -o image.tar $(tag)'
    - task: CopyFilesOverSSH@0
      inputs:
        sshEndpoint: 'OTA-Live-VM SSH'
        contents: 'image.tar'
        targetFolder: '/tmp/'
        readyTimeout: '20000'
        failOnEmptySource: true
    - task: SSH@0
      inputs:
        sshEndpoint: 'OTA-Live-VM SSH'
        runOptions: 'script'
        scriptPath: '~/otaliveupdater.sh'
        readyTimeout: '20000'